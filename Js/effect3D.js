// Efecto en 3D de las tarjetas
VanillaTilt.init(document.querySelectorAll(".card"), {
    max: 25,
    speed: 400
});

VanillaTilt.init(document.querySelector(".imagen-information"), {
    max: 25,
    speed: 400
});

VanillaTilt.init(document.querySelector(".imagen-about"), {
    max: 25,
    speed: 400
});

VanillaTilt.init(document.querySelector(".text-banner"), {
    max: 25,
    speed: 400
});

VanillaTilt.init(document.querySelector(".escudo"), {
    max: 25,
    speed: 400
});


VanillaTilt.init(document.querySelector(".sidebar"), {
    max: 5,
    speed: 400
});


VanillaTilt.init(document.querySelector(".contenedor-thumbnail"), {
    max: 25,
    speed: 400
});

